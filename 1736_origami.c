#include <stdio.h>

char a[1050000];
char t[1050000];

int main(void)
{
	int n;
	int i;

	scanf("%d", &n);
	a[0] = 'V';
	t[0] = 'V';
	for (i=1;i<n;i++) {
		for (j=) {
			
		}
		a[i] = 
	}
	return 0;
}